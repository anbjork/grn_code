
The pipeline configuration template might get out of sync. It's purpose is to show the structure of a pipeline configuration. I made it when I moved to separate code from configuration, to keep my git history and status clean from constant config changes. Because they are now separate, and I work with the one that is not tracked by git, there is a fairly large risk that the template gets out of sync. So if something seems missing, figure it out or contact me. //Anton Björk




